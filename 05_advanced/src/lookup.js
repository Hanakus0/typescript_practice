"use strict";
var _a, _b, _c;
const data = {
    id: 1,
};
console.log((_b = (_a = data.user) === null || _a === void 0 ? void 0 : _a.name) === null || _b === void 0 ? void 0 : _b.first);
const userDate = (_c = data.user) !== null && _c !== void 0 ? _c : 'no-user';
